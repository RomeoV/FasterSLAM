#include "rdtsc_benchmark.h"
#include <iostream>
#include "resample_particles.h"

int main() {
    
    // resample_particles and resample_particles_base are the same
    // so no need to have a benchmark

    return 0;
}
